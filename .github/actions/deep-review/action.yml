name: Deep Review
description: Run Codex CLI deep review prompt and capture a report.
inputs:
  codex-version:
    description: Codex CLI version.
    default: "0.5.0"
  model:
    description: Codex model to use.
    default: "gpt-5-codex"
  prompt-file:
    description: Prompt file path.
    default: "prompts/deep-review.md"
  output-path:
    description: Report path relative to workspace.
    default: "artifacts/deep-review/report.md"
  extra-args:
    description: Additional CLI args (newline separated, optional).
    default: ""
outputs:
  report-path:
    description: Location of generated report.
    value: ${{ steps.run.outputs.report_path }}
runs:
  using: "composite"
  steps:
    - uses: ./.github/actions/setup-codex-cli
      with:
        codex-version: ${{ inputs.codex-version }}

    - name: Run deep review
      id: run
      shell: bash
      env:
        MODEL: ${{ inputs.model }}
        PROMPT_FILE: ${{ inputs.prompt-file }}
        OUTPUT_PATH: ${{ inputs.output-path }}
        EXTRA_ARGS: ${{ inputs.extra-args }}
      run: |
        set -euo pipefail
        chmod +x scripts/run_codex_cli.sh
        if [[ -n "${EXTRA_ARGS}" ]]; then
          mapfile -t extra <<< "${EXTRA_ARGS}"
          scripts/run_codex_cli.sh "${MODEL}" "${PROMPT_FILE}" "${OUTPUT_PATH}" "deep" "${extra[@]}"
        else
          scripts/run_codex_cli.sh "${MODEL}" "${PROMPT_FILE}" "${OUTPUT_PATH}" "deep"
        fi
        echo "report_path=${OUTPUT_PATH}" >> "${GITHUB_OUTPUT}"
